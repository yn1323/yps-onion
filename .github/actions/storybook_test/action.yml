name: Storybook Test
description: Storybook Test

runs:
  using: 'composite'
  steps:
    - name: Build storybook
      run: pnpm build-storybook
      shell: bash
    - name: Storybook test
      run: pnpm test-storybook:ci
      shell: bash
